<% if @message.persisted? %>
  $(".messages").prepend("<%= j render @message %>");
  $('input#message_body').val('');
<% else %>
    $(".messages").before("<%= j render_flash_message @message.errors.full_messages.to_sentence %>");
<% end %>
